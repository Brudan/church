<% if @user.events.any? %>
      <h3>Events (<%= @user.events.count %>)</h3>
      <!--
      <ol class="events">
      -->
        <%= render @events %>
        <!--
      </ol>
  -->
      <%#= will_paginate @microposts %>
    <% end %>